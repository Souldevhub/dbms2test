/*import { createPool } from "mariadb"

const pool = createPool({
    host: 'localhost',
    user: 'anatolii',
    password: 'password1',
    database: 'test',
    connectionLimit: 10,
    connectTimeout: 5000,
});


export default Object.freeze({
    pool: pool
})
    */